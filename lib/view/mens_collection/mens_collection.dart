import 'package:flutter/material.dart';

class MensCollection extends StatelessWidget {
  const MensCollection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Container()],
      ),
    );
  }
}
